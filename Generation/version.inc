
{$ifdef VER160}
{$else}
  {$ifdef VER170}
  {$else}
    {$ifdef VER180}
    {$else}
      {$ifdef VER190}
      {$else}
        {$ifdef VER200}
        {$else}
          {$ifdef VER210}
          {$else}
            {$define DVERXE}
          {$endif}
        {$endif}
      {$endif}
    {$endif}
  {$endif}
{$endif}
